# Handoff Context

## Current State

- Repo: `I:\Ai_WorkSpace\agency-agents-zh`
- Main custom app: `my_workspace/`
- Management UI: `python my_workspace/web_app.py`
- Default URL: `http://127.0.0.1:8765`
- Local model target: Ollama-compatible API at `http://127.0.0.1:11434/v1`, default model `qwen3:8b-q4_K_M`.
- Current product mode: long-video workflow only. Daily run path is `workflow_长视频全流程`.
- Original upstream agent folders should stay untouched unless explicitly requested.
- Do not commit API keys, uploaded references, voice samples, generated task outputs, local model files, or large media assets.
- Old short-video, xiaohongshu, game, software-market, and platform-design workflows/staff are archived in place. Do not delete them unless explicitly requested.

## Current UX / Runtime Rules

- `新建任务` is only for entering the long-video requirement and starting a task.
- `任务输出` is the execution console: progress, current step details, stop, confirm/continue, rerun, output files, final video preview, and export.
- `系统配置` owns model, ComfyUI/RunningHub, TTS, FFmpeg, memory, and automation settings.
- The run page is intentionally ChatGPT-style: one main demand box plus the run button.
- `推进方式`:
  - `一键到底`: runs automatically to final output.
  - `逐步确认`: pauses after each completed employee step and waits for the user to review output.
- Running/resume/rerun should lock run-related inputs until the job is completed, failed, cancelled, or paused.
- Refresh/browser-exit should pause active jobs so the task can resume from `任务输出`.
- Task output should auto-select the latest task when no task is selected.
- Progress panel should show the current step by default; details are collapsed and expandable.
- When `显示调试文件` is off, the top file tabs should only show `input.md` and `final_output.md`; step outputs remain available in the dedicated step-output list.
- The task-output summary card strip is intentionally hidden to avoid duplicate task/workflow/final-status information.

## Production Pipeline

- Long-video workflow ends with editing/composition as the final authority.
- ComfyUI/RunningHub is for visual material generation or preview clips, not final subtitle/audio burning by default.
- Audio and subtitles belong to the voice/subtitle and editing employees:
  - `20_语音字幕包装师`
  - `22_剪辑成片执行师`
- ComfyUI material scheduling is now integrated into:
  - `06_分镜生图设计师` for `image_prompts`
  - `07_视频生成执行员` for `video_prompts`
- Final composition is handled locally by FFmpeg when configured.
- `my_memory` should normally be injected only into video-output stages, not every employee step.
- In `comfy_full` mode, the engine runs a hard ComfyUI material gate after the material step. For the current long-video workflow, this is now the `07_视频生成执行员` step; older workflows with `21_` still gate there for compatibility.
- The material gate only passes when the ComfyUI adapter reports success, has downloaded files, and its manifest has `success_count == job_count` and `failed_count == 0`.
- If the material gate fails, the workflow must stop at the ComfyUI material step and must not enter the final editing step.
- RunningHub/ComfyUI endpoint placeholders such as `/run/workflow/keep` are invalid and should be surfaced as configuration errors.

## Recent Fixes

- [2026-07-02] Final packaging now reuses a completed ComfyUI material gate instead of submitting the same visual DAG again. `production_pipeline.py` snapshots the prior production plan/manifest before recompilation, compares a stable fingerprint of `visual_jobs`, verifies every required cached output still exists, preserves optional skipped jobs and asset provenance, then continues directly to subtitles/TTS/BGM/FFmpeg. Changed prompts, missing files, failed jobs, or a different visual plan still invalidate the cache and trigger normal visual execution. Audio packaging extraction now also reads the employee contract fields `audio_package.voiceover_text`, `audio_package.subtitle_srt_draft`, and `production_intents.audio[*].subtitle_segments`, so valid structured subtitles no longer degrade into a one-line placeholder merely because the employee did not emit a separate fenced `srt` block.

- [2026-07-02] Vertical delivery defaults now follow the locked aspect ratio. `normalize_global_context()` chooses `1080x1920` for `9:16`, `1080x1080` for `1:1`, and `1920x1080` for `16:9` before the parameter policy locks delivery dimensions, preventing a vertical `480x848` visual chain from being packaged into a mistakenly locked horizontal canvas.

- [2026-07-01] ComfyUI/RunningHub credential sync now treats the System Config fields and ComfyUI Debug fields as one effective runtime configuration. Frontend production run/resume/retry, MCP checks, and task Comfy debug actions now use the current debug API Key/Base URL when present, then fall back to the system fields and saved browser settings. Saving settings preserves a previously saved Comfy API Key when the visible password field is blank, and backend run/retry handlers no longer overwrite production compose credentials with empty request values before applying the runtime cache. This fixes the case where the user filled credentials in the debug console but production status still reported `missing_runninghub_api_key`.

- [2026-07-01] Video enhancement is now explicitly skippable across the production graph boundary. `production_graph.json` preserves `optional_when_unconfigured`, production manifest nodes retain that flag when material jobs are upserted, and the Task State Center filters optional missing workflow slots by the explicit flag before falling back to legacy `enhance_video` / `video_enhance` keyword detection. This keeps `11_video_enhance/video_upscale` from blocking full-flow tests when the enhancement workflow is not configured, while still allowing it to run normally once configured.

- [2026-07-01] Task Output next-action guidance now prioritizes real production blockers before export/review. If a completed employee run has no final media and the visual provider is blocked by missing ComfyUI workflow slots or a missing RunningHub API Key, the recommended operation is to configure those items and retry material generation. Diagnostics also call out a missing RunningHub API Key explicitly while keeping optional video enhancement slots filtered out.

- [2026-07-01] Existing-task material retry now inherits the task's saved material-capable mode (including `composition.execution_mode` after a previously failed retry overwrote top-level mode) when the current UI happens to be set to `package_only`, preventing `Cannot retry material job when production mode is 'package_only'`. Before invoking the visual provider, retry recompiles `production_plan.json`, `comfyui_payload.json`, and `production_graph.json` from persisted 01/06/07/20/22 employee outputs, so compiler routing fixes also repair older tasks instead of replaying stale archived workflow IDs. Completed employee runs also clear stale workflow error/traceback fields when production status is refreshed, avoiding an obsolete compiler exception being shown beside the current production diagnostic.

- [2026-07-01] Production-plan compilation now treats employee `compatibility.recommended_workflow_*` values as legacy hints instead of authoritative routing. Known image/video intents map to the active function-based ComfyUI debug-console slots (`01_base_asset_image`, `03_style_cover_image`, `04_keyframe`, `06_i2v_first_frame`, and related modes); base assets also select character/product/scene modes from `asset_role`. This prevents completed employee JSON containing archived IDs such as `01_all_in_one_image`, `04_keyframe_image`, or `06A_i2v_first_frame` from being falsely reported as unconfigured. Optional video enhancement maps to `11_video_enhance/video_upscale` and remains skippable when that slot is not configured.

- [2026-07-01] Task status now exposes a runtime-model sync diagnostic. When a task can be resumed but there is no backend runtime model cache and no active run, `/api/task-status` adds `runtime_model_not_synced`, sets the next action to `sync_runtime_model`, and explains that the user should refresh/save/test model config or continue with the visible form values. This prevents the Task Output page from looking silently ready while the backend would still reject resume for missing model/API Key.

- [2026-07-01] The management page now automatically syncs remote runtime model settings from browser localStorage to the backend cache during `loadConfig()`. This covers the common case where the user had already filled DeepSeek/custom model settings before the `/api/runtime-model-config` endpoint existed, then refreshed after a service restart. The auto-sync is intentionally remote-only: it skips `api_key=local` and the Ollama base URL, so a stale local offline setting will not be silently cached as the runtime model. Explicit run/resume/rerun still submit the currently visible form values.

- [2026-07-01] Resume now self-heals previously rejected employee outputs when validator/compiler fixes make them valid. During `WorkflowEngine.resume()`, the current step checks `output_rejected_retry_*.md` / `output_rejected_*.md` with the current requirement and production-contract validators before making a new model call. A passing candidate is promoted back to `output.md`, validation files are rewritten, stale `error.json` is removed, and the normal post-step path still runs. For `comfy_full` tasks, `_first_incomplete_step()` now keeps the resume point on the 07 visual material step when that step has valid output but no `production_manifest.json`, so a recovered video employee output still triggers the ComfyUI/RunningHub material gate instead of skipping straight to downstream editing.

- [2026-07-01] Fixed a model-config persistence gap where the System Config page could show a filled DeepSeek/custom model setup while the backend runtime cache was still empty unless the user clicked the model-test button. The frontend now syncs the runtime model config to `/api/runtime-model-config` on settings save and immediately before new-run/resume/rerun calls; the backend saves provider/model/API key/base URL/timeout but only returns a `has_api_key` flag. Initialization no longer restores localStorage a second time after loading backend config, preventing saved runtime model values from being overwritten by stale browser settings.

- [2026-07-01] Fixed a frontend recursion that could break the Task Output page after switching views. `syncOutputButtons()` called idle-unlock logic, which called `trackRun("")`, which called `syncOutputButtons()` again until Chrome reported `Maximum call stack size exceeded`. Clearing tracked run state is now separated from full button resync, and `setWorkflowInteractionLocked()` can skip nested output sync when called from that idle-unlock path.

- [2026-07-01] Model execution now has a no-silent-local-fallback guard. `/api/run`, `/api/resume-task`, and `/api/rerun-step` resolve the runtime model config first and then require a model plus API key from either the request, cached runtime config, or environment. If none exists, the request fails with a clear Chinese message telling the user to configure/test the model first, instead of silently falling back to the local offline Qwen path.

- [2026-07-01] Resume/rerun no longer depend only on one browser tab's localStorage for model settings. The management app now caches the most recent explicit runtime model config (`provider` / `model` / `api_key` / `base_url` / `timeout`) in local ignored state under `tmp/`, `loadConfig()` hydrates empty model fields from that cached config, and `/api/run`, `/api/resume-task`, `/api/rerun-step`, and `/api/test-model` all resolve blank requests through the saved runtime config before falling back. This prevents `继续任务` from silently dropping back to the local offline `qwen3:8b-q4_K_M` path after refresh, browser-context changes, or service restart.

- [2026-07-01] Fixed the next resume blocker after step 5. Staff 07's compatibility `video_prompts` can now inherit first/middle/last frame references from the same `production_intents.video` object when the old compatibility rows omit explicit frame IDs, so valid three-frame outputs are no longer rejected as missing references. `production_plan_compiler.py` now prefers compiler-generated `image_prompts` / `video_prompts` as the authoritative DAG input while preserving raw staff compatibility rows under `legacy_*`, ensuring compiled jobs keep their explicit `depends_on` and `input_bindings`. Resume failures also now clear stale pause/confirmation/block flags and rewrite `run_summary.json`, so Task Output shows the latest failed step instead of an old error after `继续任务`.

- [2026-07-01] Browser verification confirmed failed employee runs now unlock top navigation, hide the stop button, and expose only the valid continue action. The production execution stage card now uses neutral stage names (`06 图片生产意图`, `生产计划编译`, `视觉后端调用`, `图片生成`, `TTS 配音`, and so on) so a waiting stage no longer misleadingly reads as already compiled or currently generating.

- [2026-07-01] DeepSeek full-flow browser testing found that staff 20 correctly emitted all subtitle rows under the documented `build_subtitles.subtitle_segments` field, while the validator only counted the shorter `segments` alias and therefore rejected two otherwise complete outputs as less than 90% coverage. Audio contract validation now accepts both names and continues using the normalized list for timestamps and coverage.

- [2026-07-01] Browser end-to-end smoke testing found that local Qwen3 employee calls could spend several minutes in default thinking mode because the OpenAI-compatible Ollama request had no local reasoning/output controls. `CodexAPI` now sends `think=false` and `max_tokens=4096` only to local Ollama endpoints. The same pass keeps top navigation enabled while a run locks task/config inputs, treats future `pending` workflow steps as waiting rather than running in the production-stage card, and no longer marks the production plan compiled merely because placeholder production status cards exist.

- [2026-07-01] Failed workflow runs are now treated as terminal immediately across persistence, the task state center, and the management UI. Failure clears stale pause/cancel/confirmation/block fields, marks the current step failed even when old confirmation metadata remains, removes `cancel`/`confirm_step` from allowed actions, unlocks the UI, and hides the stop button. Historical failed summaries with stale `awaiting_confirmation` data are also read safely as failed terminal tasks.

- ComfyUI 调试台分类按生产顺序由 10 组精简为 7 组：`01 基础资产`、`02 分镜关键帧`、`03 图片处理`、`04 视频生成`、`05 视频控制`、`06 数字人口播`、`07 视频后期`。只改变导航归类和显示顺序，不改变子模式值、独立配置或员工兼容路由。
- ComfyUI 调试台的功能组标题可点击展开/收起，箭头和 `aria-expanded` 会同步变化；每次刷新或重新打开页面时，所有功能组统一恢复为默认收起，当前页面内仍可自由展开。
- ComfyUI 调试台改为按功能组织的两级树（功能组 → 可运行子模式），桌面端不再重复显示子模式下拉框。每个叶子子模式独立保存接口、nodeInfoList、尺寸、提示词、参考素材、运行状态与结果；旧的工作流级配置和状态会作为首次迁移回退。06/07 员工现以 `capability + mode` 为主路由，并继续输出 `workflow_id + workflow_mode` 兼容现有生产链路。
- The current long-video workflow no longer runs `21_ComfyUI素材编排师` as a separate step. Its responsibilities are integrated into `06_分镜生图设计师` (`image_prompts`) and `07_视频生成执行员` (`video_prompts`). `production_pipeline.py` now merges the first JSON blocks from 06/07 into `comfyui/comfyui_payload.json`, and the ComfyUI material gate falls back to the 07 step when no 21 step exists.
- Staff 06 and 07 now explicitly require full per-shot prompt expansion: every row in the storyboard / shot list must have a matching detailed prompt section, with no "same as above", "omitted", or "key shots only" shortcuts. Staff 21 now has a fallback requirement to cover all shot numbers in `image_prompts` / `video_prompts`, and to record inferred or missing prompts in `missing_or_inferred_prompts` instead of silently dropping shots.
- The first ComfyUI template `01_image_z_image_turbo` is a single boolean-Switch image workflow: `Switch=false` routes `EmptySD3LatentImage` to KSampler for text-to-image, and `Switch=true` routes `LoadImage -> VAEEncode` to KSampler for image-to-image. The Switch node must be the Comfy Core boolean Switch with only `on_false` and `on_true` inputs, not the `Input_1` / `Path` variant. API template and RunningHub nodeInfo preset include `LoadImage.image -> {{reference_image}}` on node `12` and `Switch.switch -> {{has_reference_image}}` on node `63`.
- ComfyUI material job chaining now supports image-to-image as well as image-to-video. During the sequential material loop, image jobs with explicit `reference_image` resolve against earlier generated images; image jobs without a reference automatically use the previous generated image when available. Video jobs keep the existing generated-image pairing behavior.
- Task output editing is now allowed while a run is paused at a step-confirmation checkpoint. The UI tracks the active run status separately from `currentRunId`, so `paused` / `awaiting_confirmation` no longer disables save, rerun, or confirm controls as if the job were still running.
- ComfyUI workflow templates now carry explicit material type metadata (`image` / `video`) through the UI payload, and `CloudComfyUIAdapter` prefers that metadata when routing material jobs. This reduces reliance on Chinese/English keyword matching in slot names. README files for `01_image_z_image_turbo`, `04_broll_material`, and deprecated `06_audio_subtitle_video_preview` were rewritten to remove mojibake and clarify production boundaries.
- Auto production mode `api_ready` is now image-only: the UI label is `只生图，不生视频`, the production pipeline no longer calls the legacy video adapter in this mode, and `production_manifest.json` marks `video_generation.adapter_status` as `skipped`.
- Resume now passes `production_config` into `WorkflowEngine.resume()`, so resumed long-video jobs can still run ComfyUI, TTS, and FFmpeg production.
- Step-confirm to auto-run transition is fixed: confirming a paused step clears `awaiting_confirmation` even after the user switches `推进方式` to auto.
- Confirmation-derived `blocked_step` / `blocked_reason` are cleared together so stale waiting-confirmation state does not remain in `run_summary.json`.
- After a completed resume, task output prefers `final_output.md` instead of reopening the last employee output.
- After `resumeSelectedTask()` finishes polling, the selected task is fetched again so step outputs, product package files, and final video preview refresh from disk.
- The confirmation button text updates immediately when the user switches advance mode; auto mode shows `确认并自动跑完后续步骤`.
- Local TTS now uses managed process execution, caps long timeouts, and kills child process trees on timeout.
- Windows SAPI TTS uses PowerShell encoded commands to preserve Chinese task paths.
- FFmpeg composition can generate `long_video_final.mp4` when audio/material inputs are available.
- Progress details preserve useful RunningHub/ComfyUI/TTS/FFmpeg metadata while hiding repetitive plain step rows.
- VoxCPM2 preset/clone TTS no longer hard-caps CPU synthesis to 300/900 seconds. The adapter now honors the requested timeout and automatically raises it for long CPU scripts based on character count; the UI default is 60 minutes with 90 minutes available.
- Task output now includes an `已生成素材` section that surfaces only generated image/video media, including final videos and image/video files under generated image, video clip, or ComfyUI output folders. Media opens through `/api/media` instead of the text editor; image assets show thumbnails in the management UI.
- During active runs, the UI only auto-focuses `任务输出` while the run was just started/resumed/rerun. If the user manually switches top tabs or terminates the task, polling no longer forces the page back to `任务输出`, so other tabs remain clickable after stop/cancel.
- Auto production can run material generation/matching and local TTS in parallel during full production, because material generation depends on visual prompts while TTS depends only on voiceover text. FFmpeg still waits until both branches finish. The ComfyUI material gate path with `stop_after_comfyui=True` remains material-only and does not start TTS.
- Task detail API now returns structured `task_state`, `allowed_actions`, and `assets` fields. The management UI uses `assets.images/videos` for the generated-material section and `allowed_actions` for output action buttons, with filename-based logic retained only as a compatibility fallback.
- Task detail API also derives structured `production_jobs` from `production_manifest.json`: material generation/matching, TTS, and FFmpeg composition. The output page renders these as production status cards so the UI no longer needs to infer branch state from raw manifest files.
- The production status cards now expose first-version branch retry actions: `重试素材` (`material`), `重试配音` (`tts`), and `重新合成` (`ffmpeg`). `/api/retry-production-job` starts these as background jobs, reuses the existing production package, updates `production_manifest.json`, appends `production_job_history`, and refreshes `run_summary.json.production_status`.
- ComfyUI material status and FFmpeg composition status are now separated in the manifest via `composition.comfyui_adapter_status` / `composition.local_ffmpeg_status` while keeping legacy `adapter_status` compatibility. This prevents a successful FFmpeg retry from incorrectly overwriting the material card state.

- During an active run, the output page now silently refreshes the selected task detail every ~2 seconds after the task directory exists. This keeps `步骤输出` in sync with newly written `step_*/output.md` files without forcing the user away from the current file or tab.
- Task detail now has a first-version authoritative `task_status` object. It normalizes workflow state, per-step state, production branch state, assets, allowed actions, and diagnostics in one backend structure. `/api/task-status?name=...` returns just this state center for polling or external diagnostics, while `/api/task` keeps backward-compatible legacy fields.
- The output page now prefers `task_status.steps`, `task_status.production.jobs`, `task_status.assets`, `task_status.allowed_actions`, and `task_status.diagnostics` over front-end filename/manifest inference. Diagnostics render as cards above production jobs, so production failures and blocked reasons are visible without opening raw JSON files.
- RunningHub/ComfyUI reference-image parameter handling is hardened. Unresolved placeholders such as `generated/shot_003_keyframe.png` are no longer passed raw into `LoadImage.image`; the adapter clears unresolved references, sets `{{has_reference_image}}` false, and drops empty `image` nodeInfo entries. Relative generated image paths are resolved against the task ComfyUI output directory when present, uploaded first for RunningHub, and the returned file value is passed to `LoadImage.image`.
- The second ComfyUI workflow template `02_ltx_video_2_3` / management slot is restored to dynamic I2V output. A previous attempt wired SaveVideo to a single-image OpenPose/LTX pose-control component, which froze motion because one still image yields one static pose, and could emit noisy audio. The experimental LTX pose-control UUID component and component definitions were removed from the default output path. SaveVideo links are back to the original dynamic LTX I2V `CreateVideo` nodes (`4819 -> 4823`, `4849 -> 4852`), and RunningHub defaults no longer pass pose-control fields. Use a separate pose-to-video workflow only when there is a pose video or pose sequence.
- The same template now keeps a visible but non-output OpenPose preview branch: `6101 DWPreprocessor -> 6102 ResizeImageMaskNode -> 6103 PreviewImage`. This lets users see/control-check the extracted pose node without wiring a single still pose into final video generation. `4823/4852 SaveVideo` remain connected only to dynamic LTX I2V outputs.
- ComfyUI workflow routing is being consolidated into two API entrypoints: `全能图片` (`all_in_one_image`, material type `image`) and `全能视频` (`all_in_one_video`, material type `video`). The management UI default workflow library now exposes only these two slots. `CloudComfyUIAdapter` routes image jobs to all-in-one image and video jobs to all-in-one video by `materialTypes`, and it now passes `task_type`, `control_mode`, `duration`, and `fps` placeholders into RunningHub nodeInfo replacement. Staff 06/07 JSON examples now require `task_type` and `control_mode` but still avoid node IDs or provider-specific internals.
- The all-in-one workflow template directories now include `workflow_contract.json` and `node_info_extended_example.json`. These are the canonical build contracts for making real ComfyUI/RunningHub workflows with IPAdapter/FaceID/style/product/video-control branches. Default `runninghub_node_info_list_preset.json` files intentionally remain conservative and only contain known-safe mappings until the user exports real API JSON and calibrates real node IDs.
- The all-in-one workflow template directories now also include importable `workflow_canvas.json` files. `01_all_in_one_image/workflow_canvas.json` is based on the verified Z-Image image canvas, and `02_all_in_one_video/workflow_canvas.json` is based on the verified LTX dynamic I2V canvas. These are ComfyUI canvas import files; `api_template.json` is only for API/RunningHub reference, not for canvas import.

- The management UI `自动化与成片配置` panel has been regrouped into clearer cards for flow switches, ComfyUI connection, two-workflow routing, node mapping, material quality gate, local voice, and advanced voice settings. `web_app.py` now uses `.automation-config`, `.config-card`, `.workflow-summary-card`, and `.mapping-card` styles for this panel.

- Terminating a run now performs an immediate optimistic UI unlock on the frontend: it stores the run ID to cancel, clears currentRunId/progressTimer, disables output auto-focus, resets the run button, and explicitly re-enables top navigation before awaiting `/api/cancel-run`. This prevents the UI from staying stuck on Task Output while a backend/model/RunningHub request is still unwinding.

- Generated assets preview in the output page now uses a gallery/lightbox UI instead of cramped file buttons: image/video cards render in an adaptive grid, clicking opens an in-page preview overlay, and users can browse with previous/next buttons or keyboard left/right, with Esc to close.
- New task creation no longer includes the `内容栏目` selector or built-in column template injection. The main composer now only uses the user's long-video demand plus reusable-asset context. The remaining `内容栏目` field was also removed from `my_custom_staff/01_需求拆解专员/agent.md` so new Brief outputs no longer carry the old column-project field.
- New task creation can now use a ComfyUI manual debug gate. In `comfy_full` mode the default `ComfyUI 调试门禁` setting pauses the long-video workflow at the material gate after 06/07 have produced `comfyui/comfyui_payload.json`. The task output page shows a `ComfyUI 调试队列` sorted by the debug-tab workflow order. Users run the current item, review generated media, click `满意，下一项`, and only after every item is approved should they continue the main workflow. The final production pass reuses the approved debug media instead of automatically rerunning the ComfyUI material branch.
- Task-bound ComfyUI debug queue runs write media into the same task under `comfyui/manual_debug/<run>/<workflow>/...` and return files relative to the current task, so the output page generated-material gallery shows them in the current task instead of under the separate `__comfy_debug__` task. The standalone ComfyUI debug tab still uses `__comfy_debug__`.
- The task-bound ComfyUI debug queue is staged: after staff 06 it shows/runs only image items, ordered as base assets, turnaround, keyframes, then later image utilities; after approved image assets are injected into staff 07 context. After staff 07 it shows/runs video items in the configured debug order. Queue card CSS uses a dedicated two-column/stacked layout so run/approve buttons do not squeeze titles.
- The task-bound ComfyUI debug queue container no longer reuses `.output-link-list`. It renders as `.task-comfy-debug-list` with standalone flex cards, fixed minimum card height, separate text/actions areas, and mobile stacking to avoid the cramped/overlapped layout seen in the task output page.
- Task-bound ComfyUI debug prompt extraction now prefers clean visual prompt fields in this order: `positive`, `positive_prompt`, `image_prompt`, `video_prompt`, `visual_prompt`, `prompt`, `description`. If a prompt field is JSON text, it extracts the same clean visual field from the JSON instead of sending the whole JSON, preventing usage/dependency notes from being rendered into generated images. Manual debug state now records `prompt_version: 2`; old approved items without this marker are surfaced as failed/re-run-needed. Lost in-memory `running` jobs are also downgraded to failed so the user can click run again.
- Task-bound ComfyUI debug queue now renders and operates by workflow/mode groups rather than individual prompt rows. For example, `01_base_asset_image/base_asset` is one group containing all base asset images; clicking run executes every runnable child in that group, and `满意，下一组` approves all successful children together before advancing to the next group. Already approved groups cannot be rerun from the normal queue; failed/completed groups can be rerun as a whole.
- Re-running a task-bound ComfyUI debug group deletes old generated files recorded by that group's child items before launching the new group run, so the generated-material gallery does not keep stale versions for the same group. Group progress counts children with completed/success/approved status or downloaded files, and the frontend renders a progress bar under each group card.
- Staff 06 now treats `02_turnaround` as a fixed four-view workflow. For each character/product it should emit one `image_prompts` item with `workflow_id=02_turnaround`, the proper `workflow_mode` / `asset_tag`, `reference_image` pointing to the base asset, and a clean business-level `positive` prompt asking for front/left/right/back four views. It must not split the four views into multiple prompt rows or write internal RunningHub/ComfyUI node parameters.
- Step-confirmation UI is hardened against tab switching. Returning to `任务输出` now refreshes the selected task detail even when a task is already selected. The confirmation bar shows whenever `run_summary.json.awaiting_confirmation` is true, even if the current file is not the paused step output. Polling also treats a transient `failed` job carrying `awaiting_confirmation` / `等待确认` as a checkpoint pause, and completed callbacks no longer overwrite an awaiting-confirmation job state.
- The management UI now has a dedicated `素材库` tab backed by `my_workspace/my_asset_library/library.json`. Generated image/video cards can be `收藏复用`; favorites are copied into the local asset library and shown as reusable cards. Before starting a workflow, the frontend appends a reusable-asset context list so downstream staff can prefer existing good assets instead of regenerating everything.
- Generated asset cards now show a dedicated high-z-index favorite button, mark already favorited source assets as `已收藏`, avoid duplicate library copies, and the asset lightbox closes from the close button, Esc, or any non-media/non-button blank area.
- Asset favoriting was moved out of the thumbnail cards and into the lightbox toolbar. Thumbnail cards now only open preview and show an `已收藏` badge when applicable; this avoids messy overlapping `收藏复用` buttons in dense generated-material grids. The lightbox toolbar contains `收藏复用`, `新窗口打开`, and `关闭`.
- Asset favorites now support removal. `/api/unfavorite-asset` removes the asset-library index entry and deletes the copied library file. In the lightbox toolbar, unfavorited task assets show `收藏复用`; favorited task assets and library assets show `取消收藏`. Removing from the library view updates the grid and closes the lightbox if the last item is removed.
- Lightbox backdrop-close handling must use normal bubbling click handling, not capture-phase interception. Clicks on toolbar buttons, images, or videos are ignored by the closer; clicks on any non-interactive blank area close the preview.
- Generated-asset thumbnails may crop with `object-fit: cover`, but the lightbox preview must never crop portrait/vertical media. Lightbox image/video elements use natural sizing (`width/height: auto`, `max-width/max-height: 100%`) plus `object-fit: contain`, and the preview stage allows overflow scrolling as a fallback. Do not use `width: 100%; height: 100%` for lightbox media because it can still behave like a landscape crop in the current grid layout.
- Text-bearing cards must not use single-line ellipsis for primary labels. Staff/workflow cards, progress-step titles, output links, and asset-card labels now allow wrapping with `white-space: normal`, `overflow-wrap: anywhere`, and visible overflow so Chinese titles like `需求拆解专员` / `口播脚本师` are not clipped vertically or horizontally.
- The management UI now has a `ComfyUI调试` tab. It exposes a first-version RunningHub/ComfyUI debug console with multi-select workflow modules, shared prompt/reference/seed/size controls, endpoint/nodeInfo overrides, synchronous `/api/comfy-debug-run` execution, and gallery/lightbox previews for downloaded image/video results. Debug outputs are stored under `my_workspace/my_task_output/__comfy_debug__/...` and served through the existing `/api/media` route.
- Built-in ComfyUI debug modules cover the recommended video-production chain: reference/keyframe generation, identity/product consistency, segmented I2V, first/last-frame transition, B-roll scene generation, video upscale, frame interpolation, inpaint/fix, background removal, and subtitle safe-area preview. Real execution still requires each slot's RunningHub endpoint and nodeInfoList to match the uploaded API workflow.
- ComfyUI debug runs merge the frontend's saved ComfyUI workflow-library payload into the backend debug workflow list, so configured `all_in_one_image` / `all_in_one_video` endpoints and nodeInfoList can be reused without retyping them in the debug tab. The tab also accepts shared endpoint/nodeInfo overrides for quick one-off tests.
- The ComfyUI debug tab reference input now has three equivalent sources: manual path, asset-library selection, and local upload. Asset-library image/video selection fills `my_workspace/my_asset_library/...` into the reference path, while uploads go through `/api/upload-comfy-debug-reference` and are stored under `my_workspace/my_reference_images/comfy_debug/` for RunningHub/ComfyUI reference resolution.
- The ComfyUI debug workflow list must keep `align-content: start` and `grid-auto-rows: max-content`; otherwise CSS grid can stretch selected workflow cards into large blank blocks in the left sidebar. Card titles use `.comfy-debug-card-title` instead of the generic `.stack` helper.
- The ComfyUI debug tab also supports one-off API JSON import. `comfyDebugApiWorkflowFile` accepts ComfyUI API workflow JSON or nodeInfoList JSON, fills the debug `nodeInfoList` override, and auto-fills Endpoint only when the imported JSON contains `/run/workflow/...` or `/run/ai-app/...`; otherwise Endpoint can remain empty to use the selected slot configuration.
- The ComfyUI debug tab is now the primary workflow configuration center. Each left-side debug workflow can be clicked as the active editor and saved independently with Endpoint, nodeInfoList, width, height, and poll timeout. Single-workflow debug runs auto-save the active config before running; multi-select debug runs ignore the shared right-side override and use each workflow's saved config from `comfyWorkflowLibrary`. System Configuration now keeps only global ComfyUI API Key/Base URL visible and hides the old workflow-template/node-mapping editor, with a button that jumps to the ComfyUI debug tab.
- ComfyUI debug API JSON import now recursively searches imported JSON for a real ComfyUI API prompt object, including RunningHub/wrapper fields such as `prompt`, `workflow`, `api`, `api_json`, `apiJson`, `template`, or JSON-string-wrapped prompt content. For debug imports it serializes all primitive mappable fields into nodeInfoList rather than only auto-enabled fields, and reports the detected field count. If the user imports ordinary ComfyUI canvas workflow JSON with top-level `nodes`, the UI now explicitly rejects it and asks for API-format export because canvas JSON lacks reliable RunningHub field names.
- ComfyUI debug workflow selection is single-select, not multi-select. The old checkbox Set state was removed; left-side cards now show a single-select marker, clicking a card switches the active workflow and loads its saved config, and the run button only runs the active workflow. This avoids stale multi-select state and checkbox-only toggling confusion.
- ComfyUI debug workflow UI state is isolated per workflow ID in `comfyDebugStateByWorkflowId`. Switching left-side workflow cards saves the current form state, loads the selected workflow's own form state, and renders that workflow's last debug results. Running a workflow writes its result preview back to that workflow's state, so users can run workflow A, switch to B, then return to A and still see A's inputs and preview.
- ComfyUI debug action buttons (`保存配置`, `清空配置`, `运行当前工作流`, `填入测试提示词`) live in the top-right of the `RunningHub 调用参数` section header. Keep them there instead of below the form so workflow-level actions are visually separated from field inputs.
- ComfyUI debug per-workflow form state now persists to browser localStorage under `SETTINGS_KEY` as `comfyDebugStateByWorkflowId`, with `activeComfyDebugWorkflowId` restored on reload. Stored fields include endpoint, reference path, prompt/negative, seed, width, height, duration, nodeInfoList, poll timeout, asset reference, reference hint, and compact last results. Results are compacted before storage to avoid localStorage quota issues.
- ComfyUI debug runs now show explicit progress feedback: the run button changes to a `run-progress` loading state, the status label says which workflow is running, the result area renders a `comfy-debug-running` placeholder with animated bar and endpoint, and failures replace the placeholder with an error card.
- ComfyUI debug `保存配置` now persists the whole form into that workflow's library item, not only endpoint/nodeInfoList/size. Saved fields include reference path, seed, duration, prompt, negative prompt, asset reference, reference hint, nodeInfoList, width, height, endpoint, and poll timeout. `defaultComfyDebugStateForWorkflow()` restores from these saved workflow fields, so users do not need to re-import API JSON after refreshing or switching workflows.
- Task Output now recognizes ComfyUI debug media under `my_task_output/__comfy_debug__/<run>/<workflow>/material_*/...` as generated assets. `_task_assets()` passes `include_nested=True` for the `__comfy_debug__` task, `_is_visible_media_asset()` accepts nested images/videos in that mode, and the frontend fallback `generatedAssetFiles()` also accepts `/material_` paths and `comfyui_result_*` filenames.
- ComfyUI debug localStorage persistence has hydration guards. `settingsRestoring` prevents restore-time defaults from writing localStorage, and `comfyDebugFormHydrated` prevents `saveCurrentComfyDebugUiState()` from overwriting restored per-workflow form state before the active workflow form has actually been loaded. This fixes browser refresh clearing debug forms.
- Asset lightbox media now height-fits the preview stage: `.asset-lightbox-stage img/video` and `.asset-lightbox-media` use `height: 100%`, `width: auto`, `max-width: 100%`, and `object-fit: contain`. This is intentional so portrait images use the available container height and scale width automatically instead of being cropped like a landscape thumbnail.
- ComfyUI debug runs are now asynchronous background jobs. `/api/comfy-debug-run` immediately creates a `comfy_debug_*` job in `RUN_JOBS`, starts `_run_comfy_debug_job()` in a daemon thread, and the frontend polls `/api/run-status`. This prevents browser `Failed to fetch` while RunningHub is still processing. Debug jobs carry `debug_type=comfy_debug` and `_active_run()` ignores them so they do not pollute the main task run state.
- The ComfyUI debug tab now treats the left workflow list as a per-workflow run dashboard. Each workflow card shows its own state (`未运行`, `运行中`, `运行完成`, `运行失败`) and running cards show the job suffix. Switching workflows preserves that workflow's form, preview, running state, and results; the top-right run button is disabled only for the currently selected workflow, so another workflow can be started while the first is still polling.
- ComfyUI debug single-workflow runs must submit exactly one RunningHub/ComfyUI job. The debug payload now sets `loop_material_prompts=False` and sends only one direct prompt field (`image_prompt` for image workflows or `video_prompt` for video workflows), not `image_prompts` / `video_prompts` arrays. This avoids the production adapter expanding one debug click into multiple material jobs such as `image_prompt`, `reference_keyframe`, and `video_prompt`.
- The long-video workflow and core staff roles were realigned to the production order: 01 Brief, 03 script, 23 storyboard/character definition, 06 reference images/keyframes with `image_prompts`, 20 TTS/subtitles, 07 segmented video/B-roll/transition/upscale/interpolation with `video_prompts`, 04 title/cover packaging, 05 final compliance, 22 edit/export. Do not split 07 into multiple workflow steps because `production_pipeline.py` reads the first `07_` output for `video_prompts` and the material gate is tied to the first 07 step.
- Staff 21 is now legacy compatibility only. The main long-video path should not run `21_ComfyUI成片编排师`; ComfyUI/RunningHub visual parameters come directly from 06 and 07. Keep 21 only for old tasks or manual fallback consolidation.

- Asset lightbox fitting is now deterministic. The stage uses flex centering and media elements default to natural `width/height:auto`; after image load or video metadata load, `fitAssetLightboxMedia()` compares media ratio to stage ratio and applies `fit-height` for portrait/tall media or `fit-width` for landscape media. Button clicks stop propagation, keyboard arrows prevent default browser handling, and blank-area close ignores toolbar/media/nav clicks. Chrome headless validation used local `__lightbox_test__` portrait/landscape/square assets and confirmed portrait media fits inside the stage and centers without cropping.
- Asset lightbox preview source is now isolated from gallery rerenders. `renderAssetGallery()` no longer rewrites global `assetPreviewItems` / `assetPreviewTaskName`; only `openAssetLightboxFromItems()` updates the active lightbox source snapshot when the user opens a card. This fixes ComfyUI debug previews jumping into mixed `__comfy_debug__` task assets after favoriting or background refresh. Chrome validation used `__preview_isolation_test__` debug assets and confirmed background task-gallery refresh plus real favorite/unfavorite calls keep the current preview on the same result group.
- The asset library now supports first-version reusable-asset taxonomy. Built-in tags are person, product, scene, broll, cover, style, keyframe, and reference; asset cards show chips, the asset-library tab can filter by tag, and each library card has a small category/note editor backed by `/api/update-asset-metadata`. The ComfyUI debug reference selector has the same tag filter, and asset-library context injected into formal runs now includes tags and notes so staff can pick better reusable references.
- Asset-library cards now include a bottom-right delete action in the metadata editor. The button confirms with the user, then reuses the existing unfavorite/delete flow so the asset-library index entry and copied library media file are both removed.
- Step-confirmation continue no longer has a redundant browser confirm dialog. `resumeSelectedTask(options)` now supports `skipConfirm`, and the dedicated `确认并继续下一步` button calls it with `skipConfirm: true`; the general resume button keeps its safety confirm.
- The first ComfyUI debug/preproduction image module is now explicitly dual-mode: without a reference image it should generate base character/product/scene assets (`task_type=base_assets`, `control_mode=none`); with a reference image it should generate keyframes for later image-to-video (`task_type=keyframe`, `control_mode=reference_image`). Staff 06 and the long-video workflow step 4 were updated to reflect the same split so no-reference runs do not pretend to be reference-image keyframe generation.
- The first ComfyUI image module has been upgraded from dual-mode to explicit task-mode routing. The debug UI now has `comfyDebugImageTaskType` with extensible modes: character_generation, product_generation, scene_generation, character_turnaround, product_turnaround, keyframe, cover_key_visual, style_reference, and inpaint_fix. Runs submit `task_type`, `image_task_mode`, `control_mode`, and `reference_image`; modes that require reference images are blocked client-side if no reference is selected. Backend debug runs validate the same modes and pass `image_task_mode` through to RunningHub/ComfyUI. `CloudComfyUIAdapter` now supports the `{{image_task_mode}}` placeholder, and production merging/Staff 06 instructions were updated to keep this field.
- The ComfyUI debug tab now exposes exactly 21 production slots: 01 character base, 02 product base, 03 scene base, 04 character turnaround, 05 product turnaround, 06 style reference, 07 keyframe, 08 cover key visual, 09 image inpaint/fix, 10 background removal, 11 first-frame I2V, 12 first/last-frame I2V, 13 live-to-anime, 14 motion transfer, 15 talking image, 16 B-roll scene video, 17 empty/transition video, 18 video upscale, 19 frame interpolation, 20 deflicker/stabilize, and 21 video inpaint/fix. Each slot has an `asset_tag`; debug result previews carry this tag, so favoriting automatically stores the copied file under `my_asset_library/<numbered_tag_folder>/...`. Updating an asset's category later moves the file to the matching folder. A temporary keyframe favorite/unfavorite test confirmed `keyframe` maps to `my_asset_library/07_keyframe/...` and deletion removes the copied file and index entry.
- After the 1-21 split, the old visible `图片任务模式` dropdown was removed from the debug form. `image_task_mode/task_type/control_mode` are still submitted internally, but they are derived from the selected left-side workflow slot's defaults instead of user selection. This avoids duplicated task-type controls now that each slot represents one concrete task type.
- Staff 06 and 07 prompts were tightened to match the 1-21 ComfyUI debug slots. Staff 06 image tasks must now include `workflow_id`, `asset_tag`, `task_type`, `image_task_mode`, `control_mode`, `reference_required`, reference source/dependencies, and `save_to_asset_tag`, routing explicitly to 01-10 image slots. Staff 07 video tasks must include the same routing fields plus video-specific references such as `last_frame_image`, `reference_video`, `reference_audio`, and route explicitly to 11-21 video slots. Both flow_rule quality gates now require workflow_id/asset_tag and clear required-input handling through `missing_or_inferred_prompts`.
- The ComfyUI debug slots were consolidated from 21 visible workflows into 12 workflow groups with per-workflow subtypes. The UI now shows `comfyDebugWorkflowMode` as a context-specific subtype selector. Consolidated groups: 01 base asset image (character/product/scene), 02 turnaround (character/product), 03 style/cover image, 04 keyframe, 05 image repair/cutout, 06 I2V (first-frame/first-last), 07 live-to-anime, 08 motion transfer, 09 talking image, 10 B-roll/transition, 11 video enhance (upscale/interpolation/deflicker), 12 video inpaint/fix. Backend debug runs accept `workflow_mode` and derive `task_type/control_mode/asset_tag` from the selected mode. Staff 06/07 docs and quality gates were updated from 1-21 explicit slots to 12 grouped workflows plus `workflow_mode`.

## Important Files

- Web UI: `my_workspace/web_app.py`
- Workflow engine: `my_workspace/my_codex_core/workflow_engine.py`
- Production pipeline: `my_workspace/my_codex_core/production_pipeline.py`
- Cloud ComfyUI adapter: `my_workspace/my_codex_core/cloud_comfyui_adapter.py`
- Local TTS adapter: `my_workspace/my_codex_core/local_tts_adapter.py`
- Local FFmpeg adapter: `my_workspace/my_codex_core/local_ffmpeg_adapter.py`
- Custom staff: `my_workspace/my_custom_staff/`
- Workflows: `my_workspace/my_workflows/`
- Generated outputs: `my_workspace/my_task_output/`
- Voice samples: `my_workspace/my_voice_samples/`

## Useful Commands / Known Risks

```powershell
python -m py_compile my_workspace/my_codex_core/local_tts_adapter.py my_workspace/my_codex_core/workflow_engine.py my_workspace/my_codex_core/production_pipeline.py my_workspace/my_codex_core/cloud_comfyui_adapter.py my_workspace/web_app.py
$env:PYTHONIOENCODING='utf-8'; @'
from pathlib import Path
text=Path('my_workspace/web_app.py').read_text(encoding='utf-8')
start=text.index('<script>') + len('<script>')
end=text.index('</script>', start)
print(text[start:end])
'@ | python - | node --check
python my_workspace/web_app.py
```

Safe restart of the management UI:

```powershell
$port = 8765
$conns = Get-NetTCPConnection -LocalPort $port -State Listen -ErrorAction SilentlyContinue
if ($conns) {
  $ownerPids = $conns | Select-Object -ExpandProperty OwningProcess -Unique
  foreach ($ownerPid in $ownerPids) {
    Stop-Process -Id $ownerPid -Force -ErrorAction SilentlyContinue
  }
}
Start-Process -FilePath python -ArgumentList 'my_workspace/web_app.py' -WorkingDirectory (Get-Location).Path -WindowStyle Hidden
```

- VoxCPM2 CPU synthesis can be slow. Use Windows SAPI fallback or short practical timeouts when local synthesis is unreliable.
- RunningHub LTX image-to-video jobs may fail if the cloud workflow expects a different reference-image node or upload format.
- Browser localStorage may contain stale ComfyUI workflow-library slots. Reset or re-save slots if mappings look wrong.
- If a task says completed but lacks final media, inspect `production_manifest.json`, `local_ffmpeg_manifest.json`, and the ComfyUI adapter manifest first.

- [2026-06-21 20:22:23 +08:00] Task-bound ComfyUI debug dimension passing was tightened: image jobs carry only width/height, video jobs carry width/height/duration/fps. The debug UI hides duration/FPS for image workflows and exposes FPS for video workflows. Saved workflow-library defaults for size/duration/fps are passed into _run_comfy_debug, while actual node placement remains controlled by each workflow's nodeInfoList placeholders ({{width}}, {{height}}, {{duration}}, {{fps}}). Staff 06 now requires image width/height only; Staff 07 requires video width/height/duration/fps.

- [2026-06-21 21:02:11 +08:00] Added keyframe ComfyUI canvas template under workflow_library/04_keyframe_image. It is based on the existing all-in-one image canvas, dedicated to task workflow_id=04_keyframe/workflow_mode=keyframe, uses image-only placeholders (prompt, negative_prompt, reference_image, has_reference_image, width, height, seed), and includes a RunningHub nodeInfoList preset plus README.

- [2026-06-21 21:16:30 +08:00] Multi-reference image support added for keyframe/debug material flow. CloudComfyUIAdapter now expands reference_images into {{reference_image_1}}..{{reference_image_4}} and {{has_reference_image_1}}..{{has_reference_image_4}}, preserves multi-reference jobs, and uploads multiple references for task-bound debug runs. Staff 06 now requires reference_images for multi-character keyframes. The 04_keyframe_image template was updated as a multi-reference/FaceID-reserved keyframe canvas with nodeInfoList placeholders for character A/B/style/extra references.

- [2026-06-22 00:00:00 +08:00] Replaced the 04_keyframe_image default canvas with a from-scratch production-ready ComfyUI keyframe workflow. Canonical files are now `workflow_library/04_keyframe_image/workflow_canvas.json` and `runninghub_node_info_list_preset.json`, with archived named copies `keyframe_production_ready_canvas.json` and `keyframe_production_ready_nodeinfo.json`. The canvas includes model loaders, prompt/negative prompt, main img2img reference, character A/B FaceID references, scene/style IPAdapter reference, width/height/seed placeholders, latent switch, sampler, VAE decode, and SaveImage output. JSON and node/link structure were validated after replacement.

- [2026-06-22 00:00:01 +08:00] The 04_keyframe_image latent routing switch was aligned to the user's actual ComfyUI canvas node number: node 69 is now the canonical Comfy Core `Switch` node. NodeInfo maps `{{has_reference_image}}` to `69.switch`; link 29 routes `69.LATENT` into `50.KSampler.latent_image`; `20.EmptySD3LatentImage` remains on `on_false` and `21.VAEEncode` remains on `on_true`. Do not use the old node 63 / BWLESS / Path variant for this branch.

- [2026-06-22 00:00:02 +08:00] The default 04_keyframe_image canvas was changed from an all-references-required graph to a safe production graph. The canonical `workflow_canvas.json` now supports txt2img with no references, optional main img2img via `69.switch={{has_reference_image}}`, and optional Character A FaceID via `68.switch={{has_reference_image_1}}`. Character B and scene/style references are no longer wired into the default required path. The previous full multi-reference graph is preserved as `keyframe_multi_reference_canvas.json` with `keyframe_multi_reference_nodeinfo.json`; use it only when the job actually has the required multi-character/style references.

- [2026-06-22 00:00:03 +08:00] Corrected the Comfy Core `Switch` node shape in 04_keyframe_image. The switch selector is a widget value, not a left-side input socket. Nodes 68 and 69 now have only `on_false` and `on_true` inputs; their widget is still mapped through nodeInfo fieldName `switch`. Links into Switch nodes must target slot 0 for `on_false` and slot 1 for `on_true`.

- [2026-06-22 00:00:04 +08:00] Updated 04_keyframe_image to use the actual Comfy Core switch canvas type `ComfySwitchNode` instead of `Switch`. LoadImage nodes 12-15 now default to `example.png`. The default safe canvas keeps all four visible reference slots (12 main, 13 character A, 14 character B, 15 scene/style), but only 12 and 13 are wired into the safe execution path; 14/15 are exposed as slots without becoming mandatory inputs. The multi-reference canvas keeps the full wired multi-character/style graph.

- [2026-06-22 00:00:05 +08:00] Fixed RunningHub nodeInfo placeholder replacement for 04_keyframe_image reference slots. `{{reference_image}}` is now reserved for the main image on node 12. Numbered placeholders `{{reference_image_1}}`..`{{reference_image_4}}` are populated only from the explicit `reference_images` array, so a main-only reference no longer accidentally fills Character A / node 13 or enables `68.switch`. Offline payload checks covered no reference, main-only, main+character, and main+three-numbered-references cases.

- [2026-06-22 00:00:06 +08:00] Connected all four default 04_keyframe_image reference slots into optional execution branches. Node 13 / Character A is gated by `68.switch={{has_reference_image_1}}`, node 14 / Character B by `67.switch={{has_reference_image_2}}`, and node 15 / scene-style IPAdapter by `66.switch={{has_reference_image_3}}`. These are no longer dangling visual placeholders; missing references still bypass their branch instead of failing. Offline checks confirmed no-ref, main-only, main+character, and full main+A+B+style payloads map to the expected image nodeInfo and switch values.

- [2026-06-22 00:00:07 +08:00] Fixed misleading manual-ComfyUI defaults in 04_keyframe_image after a generated image looked like a character-sheet / multi-view output and ignored the reference. The default positive prompt is now a single-keyframe prompt and explicitly blocks text, labels, character sheets, turnarounds, multiple views, and collages through the negative prompt. Node 69 now defaults to true for manual ComfyUI imports so node 12's main reference affects the image when the user clicks Run directly. Optional switches 68/67/66 still default to false and should be enabled only when the corresponding A/B/style reference is intentionally loaded. RunningHub/API mode still overrides switches through nodeInfo.

- [2026-06-22 00:00:08 +08:00] Tightened 04_keyframe_image img2img reference preservation after the user confirmed the img2img branch was running but the reference still looked ineffective. The cause was an overly high default KSampler denoise of 0.72, which substantially redraws the latent and weakens the uploaded reference. Default KSampler settings are now steps=24, cfg=1.3, denoise=0.38 across the default, production-ready, and multi-reference canvases. The default prompt now explicitly asks to preserve the same subject identity, face, outfit, body proportion, and main pose from the reference image, while the negative prompt blocks different identity, changed outfit, changed hairstyle, character sheet, turnaround, multiple views, and collage.

- [2026-06-23 00:00:00 +08:00] 04_keyframe was reset to plain txt2img. The user decided keyframe generation should not pass reference images for now. Canonical `workflow_library/04_keyframe_image/workflow_canvas.json` is now based on the user's latest `关键帧生成 (5).json` txt2img Z-Image workflow, with the disconnected VAEEncode node removed. Canonical nodeInfo maps only `63.text -> {{prompt}}`, `64.width -> {{width}}`, `64.height -> {{height}}`, `66.seed -> {{seed}}`, and `9.filename_prefix -> keyframe_txt2img`; it contains no `reference_image` or `reference_images` mapping. The ComfyUI debug workflow mode `04_keyframe/keyframe` now uses `control_mode=none` and `requires_reference=false`; `web_app.py` validation no longer rejects keyframe runs without references. Staff 06 instructions and quality gates were updated so 04_keyframe is txt2img and should not write reference fields or missing-reference blockers.

- [2026-06-23 00:00:01 +08:00] Fixed task cancellation while waiting for step confirmation/manual ComfyUI confirmation. The output page now exposes `cancel` as an allowed action for `awaiting_confirmation` and `blocked` task states, and the output cancel button is shown when either an active run is running or the selected task can be cancelled. `cancelCurrentRun()` can now call `/api/cancel-run` with `task_name` when `currentRunId` has been cleared at a confirmation checkpoint. Backend `_stop_run()` can resolve a job by task name, allows `paused`/awaiting-confirmation jobs to transition to `cancelled`, clears `awaiting_confirmation` fields on the job and `run_summary.json`, and marks the summary status cancelled. Python compile and extracted frontend `node --check` both passed.

- [2026-06-29 00:00:00 +08:00] Added an independent experimental `06_i2v_first_middle_last_frame` LTX 2.3 workflow without replacing the stable 06A/06B entries. The adapter and debug paths preserve explicit start/middle/end reference order and expose `middle_frame_image` placeholders. The new 06C canvas uses start frame at 0s, middle guide frame 40, and end guide frame 72; therefore 06C is fixed at 4 seconds / 24fps (97 requested frames, 81-frame internal LTX guide length). Pre-production workflow defaults 01-10 now use the 848x480 480p working canvas, while enhancement/final editing retains responsibility for target-resolution upscale. Staff 06/07/22 rules document selective three-frame use and final upscale ownership. JSON, Python compile, extracted frontend JavaScript, canvas links, 480p defaults, and offline three-frame routing checks passed.

- [2026-06-29 00:00:01 +08:00] Fixed the standalone ComfyUI debug console run button. The frontend referenced a missing `runComfyDebugAsync` handler after the background-job refactor, so page initialization stopped before binding the click action. The handler now validates the selected workflow, builds the mode payload, starts `/api/comfy-debug-run`, stores per-workflow run state, and polls `/api/run-status`. Corrupted placeholder messages in this path were restored to readable Chinese. Browser regression confirmed that clicking with an empty prompt shows `请输入调试提示词` in both status areas with no console errors and without submitting a RunningHub job.

- [2026-06-29 00:00:02 +08:00] ComfyUI debug generation modules 01-10 now migrate legacy saved dimensions (`1920x1080`, `1080x1920`, `1280x720`, `720x1280`, `1024x576`, `576x1024`, `960x544`, `544x960`) to 480p working dimensions while preserving landscape/portrait orientation. Modes with `requires_reference=false` and `control_mode=none/broll/transition` hide and clear the reference path, asset filter, and preview grid, so the first base-asset workflow cannot accidentally submit a stale reference image. Browser regression confirmed 01 loads as 848x480 with all reference controls hidden, a simulated legacy 1920x1080 state migrates to 848x480 after reload, and 02 turnaround remains 848x480 with reference controls visible.

- [2026-06-30 00:00:00 +08:00] Deleting a task from Task Output now also deletes asset-library entries whose `source_task` exactly matches that task, removes their copied library media files, refreshes both task and asset-library views, and reports the removed asset count. Manually imported assets (`source_task` empty) and assets from other tasks remain untouched. An isolated temporary-directory test covered task directory removal, owned asset deletion, index cleanup, and preservation of unrelated/imported assets.
- [2026-06-30 00:00:01 +08:00] Local VoxCPM2 was smoke-tested with the preset voice path and local cached `openbmb/VoxCPM2` model. It successfully generated a 2,795,564-byte WAV for a short Chinese sentence, confirming the local TTS path works. Pure CPU synthesis took 465.8 seconds, so Windows SAPI remains the practical fast fallback when turnaround matters.

- [2026-06-30 00:00:02 +08:00] Staff 06/07 multi-frame routing now uses first/middle/last frames exclusively. Single-keyframe shots may still use `first_frame`, but any shot needing multi-frame control must plan `start_frame` / `middle_frame` / `end_frame` at approximately 0/2/4 seconds and route to `06_i2v_first_middle_last_frame` with 4 seconds / 24fps. Staff 06/07 must not output or schedule `first_last_frame`; the 06B workflow remains only for compatibility/manual debugging. Prompt examples and flow quality gates were updated and validated as JSON.

- [2026-06-30 00:00:03 +08:00] First-version production DAG foundation was added. `production_graph.json` and `production_job_state.json` now describe visual jobs plus independent 08 audio-visual packaging jobs, with explicit `job_id`, `depends_on`, semantic `input_bindings`, global context defaults (`848x480` working, `1920x1080` delivery, `24fps`), per-job cache hashes, upstream-output file routing, retry attempts, blocked reasons, and artifacts. `production_manifest.json` now has schema version 2, `production_nodes`, `global_context`, and artifact metadata. FFmpeg packaging stays outside ComfyUI: local TTS can run independently, `talking_image` waits for the final WAV, BGM is selected from the local asset library, and FFmpeg does voice+BGM ducking, subtitles, 1080p/24fps export. Staff 06/07 now require `job_id`, `depends_on`, `input_bindings`, `character_id`, and `style_id` while keeping legacy fields for compatibility. The debug console validates semantic slots through `nodeInfoList`, and the asset library can store audio/BGM files. Offline checks covered AST/JS/JSON, DAG cache reuse, upstream invalidation, dependency blocking, and FFmpeg ducking/filter construction.

- [2026-06-30 00:00:04 +08:00] Digital staff workflow abstraction first pass was applied. `workflow_长视频全流程` is now positioned as the “通用视频生产主流程”: 01 routes `production_type` (`drama_story`, `product_promo`, `talking_avatar`, `asset_only`, `custom`) and outputs target platform, aspect ratio, voiceover/final-video needs, and quality mode without selecting ComfyUI workflows. Staff 06/07/20/22 now use `production_intents.image/video/audio/package` as the primary contract, while retaining legacy `image_prompts`, `video_prompts`, audio package, and `edit_timeline` compatibility fields so the current production path can keep running. This stage intentionally does not implement `production_plan_compiler.py`; workflow_id/workflow_mode/asset_tag/job_id/depends_on/input_bindings remain compatibility fields rather than the employees’ primary responsibility.

- [2026-06-30 00:00:05 +08:00] The other three layers of the four-layer production architecture were added. Layer 2 lives in `my_workspace/my_production_templates/production_templates.json` and maps `production_type` to default production templates plus recommended workflow contracts. Layer 3 lives in `my_workspace/my_codex_core/production_plan_compiler.py`; it reads staff `production_intents`, selects a template, writes `production_plan.json`, and compiles compatible `image_prompts` / `video_prompts` while preserving old fields. Layer 4 is now wired through `production_pipeline.py`, which writes the production plan, uses the compiled payload for the existing ComfyUI/TTS/FFmpeg path, and records `architecture_layers` plus `selected_template` in `production_manifest.json`. `docs/PRODUCTION_ARCHITECTURE.md` documents the four-layer boundary. This is still a compatibility bridge: the existing CloudComfyUIAdapter consumes compiled legacy prompt groups, while full production_graph authority can move to `production_plan.visual_jobs` in a later phase.

- [2026-06-30 00:00:06 +08:00] Asset-library relationship metadata was added as a compatible migration. Every library entry is normalized with `asset_id`, `source_task_id`, `producer_job_id`, `parent_asset_ids`, `character_id`, `style_id`, `scene_id`, `version`, `approved`, `usage_count`, optional `derived_asset_ids`, and checksum for newly imported/favorited assets. Existing `id` and `source_task` remain for backward compatibility. Favoriting task outputs now tries to infer the producer job and parent assets from `production_manifest.json`, `production_job_state.json`, `production_graph.json`, and `production_plan.json`; ComfyUI-approved auto-favorites are marked `approved=true`. Deleting an asset or task also clears parent/derived references from remaining library records. The asset detail panel now shows a read-only “资产关系” block so provenance can be inspected without opening `library.json`.

- [2026-06-30 00:00:07 +08:00] Production entities were promoted to first-class objects. `my_workspace/my_production_entities/production_entities.json` stores `characters`, `styles`, `products`, and `scenes`; `my_workspace/my_codex_core/production_entities.py` normalizes the registry, resolves entity IDs, and enriches `global_context.resolved_entities`. `production_plan_compiler.py` now collects `character_id`, `style_id`, `product_id`, and `scene_id` from staff `production_intents`, loads matching entities, and attaches `entity_context` plus entity reference assets/constraints to compiled image/video prompt items. `CloudComfyUIAdapter` preserves `product_id`, `scene_id`, and `entity_context`, and exposes `{{product_id}}`, `{{scene_id}}`, and `{{entity_context}}` placeholders for nodeInfo. Web APIs `/api/production-entities` and `/api/save-production-entities` read/write the entity registry. Staff 01/06/07 docs now require stable entity IDs for reusable characters/styles/products/scenes instead of repeated natural-language descriptions.

- [2026-06-30 00:00:08 +08:00] Added the first version of the Task State Center. `my_workspace/my_codex_core/task_state_center.py` builds one read model for task output pages from `run_summary.json`, workflow steps, active run state, ComfyUI manual-debug status, `production_manifest.json`, DAG nodes, TTS/FFmpeg stages, assets, blockers, allowed actions, and the recommended next operation. `/api/task-status` and task detail payloads now expose this unified object through `task_status` / `state_center`, while legacy fields remain for compatibility. The frontend output overview reads the state center and shows next-action and blocker cards before production job cards, so the page no longer needs to infer status separately from scattered files.

- [2026-06-30 00:00:09 +08:00] Added task-level parameter inheritance and locking. `my_workspace/my_codex_core/production_parameter_policy.py` now normalizes `global_context.parameter_policy`, locking character identity, style, shot composition, 480p working render size, 24fps global frame rate, and first/middle/last video clips at 4 seconds / 24fps. `production_plan_compiler.py` applies the policy to employee `production_intents`, writes `parameter_overrides`, and forces compiled image/video prompts to inherit locked values. `production_graph.py` preserves parameter locks in graph jobs, while `cloud_comfyui_adapter.py` reapplies locks immediately before payload submission so stale node-level width/fps/duration values cannot leak into RunningHub/ComfyUI. Visual-chain working dimensions are now 16:9=`848x480`, 9:16=`480x848`, 1:1=`480x480`; delivery resolution remains an enhancement/export responsibility. Staff 01/06/07 and `docs/PRODUCTION_ARCHITECTURE.md` were updated to say employees should reference entities and intents, not override locked face/hair/outfit/style/resolution/FPS settings.

- [2026-06-30 00:00:10 +08:00] Added the production-plan visibility and debugging layer. Task detail payloads now include `production_plan_preview`, and the Task Output page renders a “生产计划预览” panel showing selected template, route, locked render/style values, visual jobs, dependencies, statuses, compile notes, and `parameter_overrides`. The ComfyUI debug page includes a “生产模板调试器”: paste 01/06/07/20/22 staff JSON/text and `/api/preview-production-template` compiles it without creating a task, returning the same preview model. The production graph is now generated from `production_plan.visual_jobs` in both `production_pipeline.py` and the ComfyUI adapter refresh path, instead of starting from an empty visual graph or only compatibility prompt groups. The Asset Library page now includes a production entity editor for characters/styles/products/scenes backed by `/api/production-entities` and `/api/save-production-entities`, so entity IDs and reference/constraint fields can be managed without hand-editing JSON.

- [2026-06-30 00:00:11 +08:00] The production template debugger now includes first-pass diagnostics and copyable repaired staff JSON. `/api/preview-production-template` inspects pasted 01/06/07/20/22 outputs for missing `production_type`, missing `production_intents`, missing `character_id`/`style_id`/`product_id`, incomplete start/middle/end frame sets, `generate_three_frame_i2v_clip` without matching `source_intent_ids`, `talking_image` without `generate_voiceover`, unresolved entity notes, low-level fields inside intent payloads, and parameter-lock overrides. The frontend shows diagnostic rows plus human-readable fix suggestions and expandable read-only `fixed_outputs` textareas (`route_json`, `image_json`, `video_json`, `audio_json`, `package_json`) for copy/paste. These are intended as scaffolding/debug tooling; once the full pipeline is stable they can be hidden behind an advanced/debug toggle, but should remain available for template or workflow regressions.

- [2026-06-30 00:00:12 +08:00] Fixed the first full-flow smoke-test blocker where clicking the main “start generation” button could land on the standalone `__comfy_debug__` output task. The root cause was that the Task Output view auto-selected the newest output directory while the real workflow run had not yet produced a task name, and `/api/tasks` exposed the standalone ComfyUI debug directory as a normal task. `/api/tasks` now excludes `__comfy_debug__`; formal task detail/status/production-plan-preview reject it; the frontend also filters it, refuses to select it from stale browser state, ignores debug jobs in active-run task selection, and suppresses history auto-select while a new main run is pending. Dedicated ComfyUI debug APIs remain available, so the debug console is not disabled.

- [2026-06-30 00:00:13 +08:00] Fixed the next full-flow startup failure where a formal task could fail before step 1 and only show `missing_workflow_steps`. The four-layer workflow rewrite left `workflow_长视频全流程.json` using `order` / `agent_id` step fields, while `WorkflowEngine` still required normalized `step` / `agent` fields. `WorkflowEngine` now normalizes both old and new workflow step schemas on load, so `order` and `agent_id` are accepted and written into task `workflow.json` as executable `step` / `agent` fields. `TaskStateCenter` and the legacy web status helper also normalize workflow steps so old partially-created task directories can still show their 9-step plan. Failed jobs are now exposed to the state center, `summary.status=failed` is recognized, and startup exceptions write `run_summary.json` when a task directory exists, preventing silent “empty failed task” pages.

- [2026-06-30 00:00:14 +08:00] Fixed UI controls staying disabled after a task failure. The run page previously scattered unlock logic across completed/failed/cancelled/paused branches; startup exceptions, polling exceptions, no-active-run restoration, rerun-step failures, resume failures, and retry-production-job failures could leave `autoFocusOutputDuringRun`, `currentRunId`, or `currentRunStatus` stale while the form remained locked. The frontend now uses `finishRunInteraction()` to clear timers, clear active run tracking, disable auto-focus, re-enable run/config controls, reset run-button progress, and resync output buttons for all terminal/exception paths.

- [2026-06-30 00:00:15 +08:00] Fixed a Task Output race where starting a new task could still display the old task while the new run was pending. The cause was asynchronous `selectTask(oldTask)` calls from the output auto-select/history path completing after `prepareOutputForPendingRun()` had rendered the new-task placeholder. The frontend now tracks an `outputSelectionVersion`; preparing a pending run increments it, stale task-selection responses are ignored, and task selection is blocked while a run is pending unless it is explicitly allowed by the active run (`selectActiveRunTask` / terminal job output). This keeps the output panel on the new-task placeholder until the backend reports the actual new `task_name`.

- [2026-06-30 00:00:16 +08:00] Fixed new runs getting stuck forever at “启动中 / 准备连接模型/API” before `/api/run` was sent. The frontend pre-run path could block indefinitely in `ensureLocalModelReady()` because `/api/test-model` had no client-side timeout when the selected provider/base URL matched local Ollama. `ensureLocalModelReady()` now uses an 8-second `apiWithTimeout` preflight and treats timeout/failure as a non-blocking warning, allowing the task to be created so the real workflow can report any model error in task state. The main `/api/run` creation call now also has a 20-second client timeout, and startup progress text is split into model/API check, reference material handling, production config loading, and task creation so future stalls show the exact phase.

- [2026-06-30 00:00:17 +08:00] Fixed repeated task names across consecutive new runs. The first automatically derived title previously remained in the task-name input, and the next run treated that stale value as an explicit title. Automatically generated names are now marked and recalculated from each run's current demand, preferring text after `主题是` / `主题：`; user-edited names still take precedence for that run. After `/api/run` accepts the task, the title field and auto-title marker are cleared so neither automatic nor manual names leak into the next task.

- [2026-06-30 00:00:18 +08:00] Added requirement locking, drift correction, and two-level confirmation policy for employee runs. Every new/resumed task writes `task_brief.json` with the original requirement, core topic, duration, style, explicit structure constraints, and confirmation policy. Steps 1-3 receive only the compact original requirement instead of the appended asset-library/ComfyUI configuration noise; every step receives a highest-priority locked-requirement block. Model outputs are checked for core-topic retention, early-step duration/structure coverage, ungrounded commerce/brand drift, and vague non-blocking `待确认` sections. A failed check saves the rejected output and automatically retries once with a correction prompt; a second failure stops the workflow before contamination reaches downstream employees. Non-direction-changing gaps must be recorded as automatically adopted defaults. Only decisions affecting theme, platform hard specs, brand/product, budget, identity, copyright/compliance, or final delivery may declare `human_confirmation_required: true`, which pauses the workflow for real human confirmation. Staff 01 now routes conceptual positive/negative narrative shorts to `drama_story`, keeps `needs_final_video=true` when the user asks for a video, and no longer emits blanket pending questions.

- [2026-06-30 00:00:19 +08:00] New-task submission no longer appends the first 30 asset-library records and their local paths to the user's original requirement. Filename/path lists gave the model no reliable visual relevance signal, enlarged every employee prompt, and could bias early planning. `runWorkflow()` now submits exactly the demand typed by the user and no longer loads the asset library before creating a task. The asset library and production-entity references remain available as independent management data; future automatic reuse should happen after the storyboard/production plan exists through structured tag, provenance, entity, style, approval, and media-analysis matching rather than raw path injection. Requirement extraction still recognizes the old `## 可复用素材库` block so existing tasks remain resumable.

- [2026-07-01 00:00:00 +08:00] Fixed controls remaining disabled after terminating a task. Two races were involved: a worker could mark the in-memory run `cancelled` before `/api/cancel-run` handled it, causing an early return that left stale `awaiting_confirmation` / `blocked_reason` fields in `run_summary.json`; and after a service restart a persisted task could still be cancelled by `task_name` even though its in-memory `run_id` no longer existed. Stop-state persistence is now idempotent, creates or normalizes `run_summary.json`, clears confirmation/block fields, and supports task-directory-only cancellation. The Task State Center and legacy status helper now prioritize terminal `cancelled` state over stale confirmation flags and do not expose workflow or ComfyUI confirmation actions for terminal tasks; cancelled tasks recommend resume instead of returning to a stale manual-debug gate. The frontend cancel flow uses `finishRunInteraction()` before the request and again after all task-list/detail refreshes, guaranteeing navigation, run/config controls, timers, and active-run tracking are unlocked even when cancellation or refresh races fail.

- [2026-07-01 00:00:01 +08:00] Added employee production-output contract validation for 03/06/20/07/22. A step now receives one automatic correction retry and then fails visibly if its output is structurally or temporally unusable: TTS/voice text must fit the target duration; image/video primary intents and compatibility prompts must share one parseable JSON object; 480p working dimensions must match the task aspect ratio; video references must resolve to real 06 intent IDs; first/middle/last clips are locked to 4 seconds/24fps with 06B/first-last modes rejected; subtitle timestamps, coverage and duration are checked; and the final edit timeline, delivery resolution/FPS and missing-assets state must be internally consistent. Validation details are written to `production_contract_validation.json`. Staff rules were updated with the same constraints, and successful initial/resumed employee workflows now explicitly write `status=completed` and `employee_workflow_status=completed` into `run_summary.json`.

- [2026-07-01 00:00:02 +08:00] Added a visual-provider layer for the production pipeline. The automation UI now exposes `visual_provider` with `runninghub`, `comfy_mcp`, and `local_comfyui` choices plus a `comfy_mcp_url` field; `production_config.compose_config` carries the selected backend through to the production manifest, and `CloudComfyUIAdapter` now prefers `visual_provider` over the old implicit provider inference. A new `visual_provider_router.py` normalizes backend selection, and `ComfyMCPAdapter` records a discovery-first placeholder manifest for Comfy MCP so the article's workflow-discovery idea can be represented in the project without breaking the current RunningHub path.

- [2026-07-01 00:00:03 +08:00] Comfy MCP was upgraded from a discovery-only placeholder into an executable visual provider. `ComfyMCPAdapter` now performs health/tool discovery, supports MCP JSON-RPC `tools/list` and `tools/call`, falls back to common REST wrappers, polls optional status/output tools, and normalizes returned URL/data-URL/local-file artifacts into durable task output files. `/api/test-comfy-mcp` exposes a safe discovery-only connectivity check, and the automation UI label now says Comfy MCP can auto-discover/execute instead of only discovering templates.

- [2026-07-01 00:00:04 +08:00] The two-step Comfy MCP integration is now exposed in the UI. Step 1 remains direct execution through the production visual provider, with a new management-panel `测试 MCP 连接` button backed by `/api/test-comfy-mcp`. Step 2 adds workflow discovery/management: `ComfyMCPAdapter.discover_workflows()` calls common workflow/template listing tools or REST endpoints, normalizes discovered workflows by id/name/material type/schema/inputs/outputs, and `/api/sync-comfy-mcp-workflows` saves them to `comfyui_workflows/mcp_discovered_workflows.json` without overwriting the calibrated local workflow library.

- [2026-07-01 00:00:05 +08:00] Fixed the new-task start view regression where clicking `开始生成` could stay on the New Task page instead of switching to Task Output. The idle-unlock guard now preserves `autoFocusOutputDuringRun` during the short pending-start window before `/api/run` returns a `run_id`, and `runWorkflow()` uses `enterPendingRunOutput()` to render the pending output placeholder, switch to Task Output, and perform a short delayed re-check so async UI refreshes cannot leave the user on the composer page.

- [2026-07-01 00:00:06 +08:00] Restored the Task Output `终止任务` button for running tasks after refresh/service-restart scenarios. The output cancel button is now controlled by task detail/state-center status instead of only the browser-held `currentRunId`, and it remains available when a task is `running`, `queued`, `paused`, awaiting confirmation, or blocked. `cancelCurrentRun()` can now cancel by selected `task_name` when the active run id is missing, while edit/rerun/resume controls still stay locked during genuinely active execution.

- [2026-07-01 00:00:07 +08:00] Added a Task Output “生产执行层阶段” card. It appears immediately during pending task startup and then updates from `task_status`, production jobs, production-plan preview, TTS/FFmpeg status, and generated asset counts. The card shows the chain from employee planning, waiting for 06/07 outputs, production-plan compilation, Comfy MCP/RunningHub invocation, image/video generation, TTS, FFmpeg, and final completion so users can see which layer is active instead of treating production as a black box.

- [2026-07-01 00:00:08 +08:00] Fixed a step-5 audio/subtitle validation crash for staff `20_语音字幕包装师`. `production_output_validator._validate_audio()` previously iterated `subtitles.get("segments")` in a generator expression before confirming it was a list, so `segments: null` raised `TypeError: 'NoneType' object is not iterable`. Subtitle segments are now normalized to an empty list, non-object segment rows are ignored with a validation issue, and missing `segments` plus missing SRT draft is reported as a normal contract issue. Resuming the task will now trigger correction/visible validation instead of crashing the workflow engine.

- [2026-07-01 00:00:09 +08:00] Existing-task material retry now preserves non-empty ComfyUI/RunningHub credentials and base URLs already stored in the task production config when the retry payload sends blank top-level override fields. This prevents a retry from accidentally erasing the frontend's resolved RunningHub default base URL and then failing before any provider call. Production-plan recompilation also routes `generate_cover_key_visual` intents with `asset_role=style/style_reference` to `03_style_cover_image/style_reference` instead of the cover-only KV mode, so style master images are checked against the correct debug-console slot. Video enhancement remains optional/skippable when `11_video_enhance/video_upscale` is unconfigured.

- [2026-07-01 00:00:10 +08:00] The Task Output production-stage card now treats `skipped` as its own visible state instead of folding it into waiting/running, and a skipped visual provider also marks image/video generation stages as skipped when no assets exist. The final completion stage now requires an actual final/video MP4 or video asset instead of treating employee-text completion or `final_output.md` as final media, so a task with missing ComfyUI slots no longer says the final product was generated.

- [2026-07-01 00:00:11 +08:00] Production-stage status normalization now treats `pending` as waiting rather than running. This keeps idle TTS/FFmpeg packaging branches from looking active while the visual provider is skipped or blocked on missing ComfyUI debug-console workflow slots.

- [2026-07-01 00:00:12 +08:00] Missing ComfyUI workflow-slot diagnostics now include user-facing Chinese debug-console paths plus the raw `workflow_id / mode`. The state center maps common production slots such as `01_base_asset_image/scene_base`, `03_style_cover_image/style_reference`, `04_keyframe/keyframe`, and `06_i2v_first_frame/i2v_first_frame` to the corresponding ComfyUI 调试台 category/submode names and attaches structured `details` plus a retry suggestion. This keeps the system from inventing fallback endpoints while making the required manual configuration precise.

- [2026-07-01 00:00:13 +08:00] Task Output diagnostics for missing ComfyUI slots now render each missing slot as an actionable row with a `去配置` button. Clicking it switches to the ComfyUI 调试台 and preselects the target `workflow_id / mode` so users can paste/save the endpoint and nodeInfoList without manually hunting through the tree. This is UI-only; it does not synthesize or guess endpoints.

- [2026-07-01 00:00:14 +08:00] Fixed the first-version `去配置` jump so the target submode is authoritative. The ComfyUI 调试台 now prefers the requested `workflow_id / mode` over the previously selected mode when rendering the submode dropdown and state preview, and prepares the target leaf state from that mode's saved slot config. This prevents a click for `01_base_asset_image / scene_base` from opening the parent workflow while still showing the old `character_base` endpoint.
