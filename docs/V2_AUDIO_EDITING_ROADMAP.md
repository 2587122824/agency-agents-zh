# V2 配音与剪辑完善路线图

> 状态：活跃实施基线  
> 更新日期：2026-07-26  
> 范围：配音、字幕、时间线编辑、BGM、音频质检、授权声音复刻与重试

## 1. 目标与边界

本路线图把“能生成一条旁白并合成成片”继续完善为可选择、可试听、可编辑、可质检、可重试且可审计的音频剪辑闭环。页面可见不等于能力可用；完成状态必须同时具备版本化合同、确定性校验、自动化测试，以及涉及真实供应商时的真实调用证据。

开发环境不兼容旧合同或旧开发数据。合同变化通过显式迁移或重建测试数据落地，不在运行时代码中增加旧版兼容分支。所有付费调用、声音复刻授权、重试费用和最终人工放行均由用户显式确认。

## 2. 七项能力验收矩阵

| 能力 | 当前状态 | 已有证据 | 完成验收 |
|---|---|---|---|
| 真实 CosyVoice 验证 | 验收工具已完成，凭据阻塞 | `cosyvoice-v1 / longxiaochun / WAV / 24000Hz` 路由、Bearer 鉴权、冻结文本与参数；正式验收命令默认只读，只有 `--confirm-paid-call` 才联网，报告不含密钥。Adapter 校验 WAV 签名、完整结构、采样率、声道、非空帧、大小与哈希；当前连接状态为 `credential_not_ready` | 配置 DashScope API Key 后执行一次显式授权的真实付费合成，登记项目素材并保存请求 ID、非敏感用量和文件探测证据 |
| 字幕生成、编辑、预览与烧录 | 首个确定性闭环已完成 | Creative Brief 旁白/对白按节拍编译冻结 cue；本地 Adapter 生成严格 SRT；文件探测校验序号、时间和内容；人工审核后剪辑助理加入字幕轨；剪辑台可预览 SRT；本机 FFmpeg/libass 真实烧录通过 | 后续补齐逐条字幕文字与时点修订、修订版本差异和成片同步预览；当前不允许静默改写已冻结字幕 |
| 音色选择、试听、语速、音量与时长门禁 | 合同与选择闭环已完成，真实试听凭据阻塞 | 迁移 `20260726_34` 与配置 v55 发布 5 个 CosyVoice 音色、默认/范围、时长容差和响度事实；制作准备页显式选择音色、语速、音量，快照、TTS 节点与供应商请求冻结精确值；真实 WAV 超过目标时长加容差即以 `AUDIO_DURATION_EXCEEDS_TARGET` 阻断。页面不以浏览器语音冒充供应商试听 | 配置 DashScope API Key 后增加显式付费试听与缓存审计，并以真实供应商输出复验 5 个音色；当前选择、执行和门禁合同已完成 |
| 剪辑体验：裁切、拖放、缩放、波形、转场、音量包络 | 首期闭环已完成 | `v2.timeline-contract.v2` 保存缩放与吸附粒度；剪辑台支持源/成片联动裁切、同轨拖放重排、20–400px/s 缩放、10–500ms 吸附；后端按音频内容哈希生成 `audio-waveform-cache.v1`；淡入淡出与 -60dB～12dB 音量包络经后端校验并由 FFmpeg 实际执行 | 后续可增加多轨联动选择、键盘精修和交叉叠化；当前已完成本路线图要求的单轨交互与确定性执行边界 |
| BGM、ducking、响度与削波控制 | 待实施 | 当前仅支持旁白音轨混合 | 增加已授权 BGM 素材与循环/裁切合同；旁白区间自动 ducking 只按冻结参数执行；输出执行响度目标、true peak/削波门禁并保存确定性证据 |
| 确定性音频 QC 与人工确认 | 部分完成 | WAV 文件签名与时长探测、正式人工 QC 报告和批准门禁已存在 | 增加采样率、声道、静音比例、连续静音、峰值削波、响度和目标时长规则；规则通过后仍需用户试听并显式确认，不以自动指标替代内容判断 |
| 授权声音复刻、精确重试与依赖级批量重试 | 待实施 | 单个失败生产 WorkItem 已支持费用确认后的原请求精确重试 | 声音样本必须绑定授权主体、授权范围、期限和不可变声音版本；TTS/字幕允许精确重试；依赖级批量重试先展示受影响节点、冻结请求、预计费用和已有成功产物，用户确认后只创建明确的新 Attempt |

## 3. 字幕闭环合同

旁白模式必须同时创建 `project.voiceover` 与 `project.subtitles`，二者都是 `project.timeline` 的必需父节点。静音模式不得创建其中任一节点。

字幕 cue 只来源于已确认 Creative Brief 的 `voiceover/dialogue` 脚本段和 `narrative_beats.target_duration_ms`。生成器按节拍顺序切分文本，保证 cue 非空、互不重叠、时间严格递增且不超过成片时长。输出为 UTF-8 SRT，并登记内容哈希、cue 数和最终时点。

字幕素材必须先通过确定性 SRT 探测和显式人工内容审核。旁白剪辑项目要求当前快照恰好一条已批准配音和一份已批准字幕；剪辑助理将它们分别冻结到音频轨和字幕轨。字幕轨当前只允许从零开始、无空隙、单条 `burn_in` 渲染。

本机渲染器使用 libass `subtitles` 滤镜与冻结样式烧录字幕。Windows 环境从字幕文件所在目录启动 FFmpeg 并仅传字幕文件名，避免盘符冒号被滤镜解析为选项分隔符。输入文件在渲染前继续执行路径、哈希和合同复验。

## 4. 实施顺序

1. CosyVoice 真实验收命令已完成；缺少 API Key 时明确拒绝，不创建费用或伪造成功证据。配置凭据后仍需执行一次显式授权的真实付费验收。
2. 音色、语速、音量和期望时长已升级为版本化音频执行合同；制作准备选择、请求冻结和时长门禁已完成，真实试听随 CosyVoice 凭据一并验收。
3. 时间线缩放、拖放、波形缓存、淡入淡出和音量包络合同已完成。
4. 当前实施：增加 BGM 素材、ducking、响度和削波控制。
5. 增加确定性 WAV QC 与人工试听确认。
6. 建立授权声音复刻实体、精确 TTS/字幕重试及依赖级费用确认批量重试。

每一步单独更新实现状态、测试基线和交接文档；不得因为后续能力尚未完成而把已完成的字幕闭环标成完整音频剪辑能力。

只读检查：

```powershell
python -m v2.scripts.validate_cosyvoice_connection
```

真实付费验收必须同时满足：已发布配置存在 API Key、`V2_EXTERNAL_PROVIDER_EXECUTION_ENABLED=true`，并由操作者显式执行：

```powershell
python -m v2.scripts.validate_cosyvoice_connection --confirm-paid-call
```

工具只输出配置/工作流标识、凭据状态、验收文本哈希、供应商请求 ID、非敏感用量和 WAV 探测事实。任何路径都不得输出 API Key；缺凭据或执行授权时 `network_probe_performed=false`。
